pre-commit:
  commands:
    fmt:
      stage_fixed: true
      glob: "**/*.go"
      run: go fmt ./...
    lint:
      glob: "**/*.go"
      run: golangci-lint run
    gitleaks:
      glob: "**/*"
      run: gitleaks git --pre-commit --staged
    hadolint:
      glob: "Dockerfile"
      run: hadolint {staged_files}

pre-push:
  commands:
    gitleaks:
      glob: "**/*"
      run: gitleaks git
